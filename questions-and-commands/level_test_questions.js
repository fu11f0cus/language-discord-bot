import { CustomQuestionBuilder } from 'discord-trivia'

export const eng_questions = [
    new CustomQuestionBuilder.Boolean()
    .setValue('The sun rises in the West?')
    .setCorrectAnswer('false'),
    new CustomQuestionBuilder.Boolean()
    .setValue('I usually goes to the gym on Mondays')
    .setCorrectAnswer('false'),
    new CustomQuestionBuilder.Boolean()
    .setValue('В английском языке существует три артикля: "a," "an," и "the."')
    .setCorrectAnswer('true'),
    new CustomQuestionBuilder.Boolean()
    .setValue('"He had been studying English for two years when he moved to London" - это предложение в Past Perfect Continuous времени.')
    .setCorrectAnswer('true'),
    new CustomQuestionBuilder.Boolean()
    .setValue('Английский алфавит содержит 26 букв')
    .setCorrectAnswer('true'),
    new CustomQuestionBuilder.Boolean()
    .setValue('"The book, which I read, was very interesting" - это пример ограничительной придаточной части')
    .setCorrectAnswer('true'),
    new CustomQuestionBuilder.Boolean()
    .setValue('Правильный порядок слов в вопросительном предложении: "You are coming to the party?"')
    .setCorrectAnswer('false'),
    new CustomQuestionBuilder.Boolean()
    .setValue('"I could care less" - это правильное выражение, обозначающее, что вас не особенно волнует что-то.')
    .setCorrectAnswer('false'),
];
export const polish_questions = [

];